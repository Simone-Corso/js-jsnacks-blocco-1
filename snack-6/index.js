//In un array sono contenuti i nomi degli invitati alla festa del grande Gatsby (createlo voi con almeno 5 nomi), chiedi all'utente il suo nome e comunicagli se può partecipare o no alla festa.

const festa = [
    'gina',
    'ginetto',
    'francesco',
    'potenza',
    'sivola'
]

for (let i = 0; i < festa + i++) function() {

    const listaGatsby = festa[i];
    console.log(i,festa);

    if (festa == listaGatsby)
        console.log(found);
    else console.log(non sei invitato);
}